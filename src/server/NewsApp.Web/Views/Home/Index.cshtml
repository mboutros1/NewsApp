@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>


@using (Ajax.BeginForm("Register", "Account", new AjaxOptions
        {
            LoadingElementDuration = 500,
            LoadingElementId = "Progress",
            InsertionMode = InsertionMode.Replace,
            OnComplete = "$('form').cleanDirty();window.location=window.location",
            HttpMethod = "POST"

        }))
{
    <input type="text" id="userId" name="userId" />
    <input type="text" id="deviceId" name="deviceId" />
    <input type="text" id="deviceType" name="deviceType" />
    <input type="submit" value="submit" />

}

@using (Ajax.BeginForm("TestSend", "Home", new AjaxOptions
        {
            LoadingElementDuration = 500,
            LoadingElementId = "Progress",
            InsertionMode = InsertionMode.Replace,
            OnComplete = "$('form').cleanDirty();window.location=window.location",
            HttpMethod = "POST"

        }))
{
    <input type="submit" value="test Send To First User" />

}
@using (Ajax.BeginForm("GetFeed", "NewsFeed", new AjaxOptions
        {
            LoadingElementDuration = 500,
            LoadingElementId = "Progress",
            InsertionMode = InsertionMode.Replace,
            OnComplete = "$('form').cleanDirty();window.location=window.location",
            HttpMethod = "POST"

        }))
{
    <input type="text" id="userId" name="userId" value ="1" />
    <input type="text" id="startAt" name="startAt" value ="0"/>
    <input type="submit" value="GetNotification" />

}

@using (Ajax.BeginForm("LoginFb", "Account", new AjaxOptions
        {
            LoadingElementDuration = 500,
            LoadingElementId = "Progress",
            InsertionMode = InsertionMode.Replace,
            OnComplete = "$('form').cleanDirty();window.location=window.location",
            HttpMethod = "POST"

        }))
{
    <input type="text" id="userId" name="userId" value="1" />
    <input type="text" id="email" name="email" value="mic_louis@hotmail.com" />
    <input type="text" id="name" name="name" value="Mike GB" />
    <input type="text" id="birthdate" name="birthdate" value="04/16/2015" />
    <input type="text" id="facebookId" name="facebookId" value="123131231231" />
    <input type="text" id="deviceId" name="deviceId" value="" />
    <input type="submit" value="Login Fb" />

}